package graphs_lab.algs

import graphs_lab.core.edges.Edge
import graphs_lab.core.graphs.Graph

fun <I, E : Edge<I>> searchCyclesInDirectedGraph(graph: Graph<I, E>) {
//	if (!graph.isDirected) throw
}
